<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="merit_res_config_settings_view_form23" model="ir.ui.view">
            <field name="name">res.config.settings</field>
            <field name="model">res.config.settings</field>
            <field name="inherit_id" ref="hr_attendance.res_config_settings_view_form" />
            <field name="arch" type="xml">
                <xpath expr="//app[@name='hr_attendance']/block[2]" position="after">
                    <block title="Check-In/Out">
                        <setting
                            title="Set True System Will Create One Day Login if one check in the check will the same check in."
                            string="One Day Login"
                            help="Set True System Will Create One Day Login if one check in the check will the same check in">
                            <field name="one_days_login" />
                        </setting>
                    </block>

                </xpath>

            </field>
        </record>
    </data>
</odoo>