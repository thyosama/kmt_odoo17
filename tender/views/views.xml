<odoo>
    <record id="product_normal_form_view_duplicate" model="ir.ui.view">
        <field name="name">product_normal_form_view_duplicate</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_normal_form_view"/>
        <field name="arch" type="xml">
            <form position="attributes">
                <attribute name="duplicate">true</attribute>
            </form>
        </field>
    </record>


    <!-- Inherit Form View to Modify it -->
    <record id="view_users_form_related_job" model="ir.ui.view">
        <field name="name">view_users_form_related_job</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">

            <group name="preferences" position="before">
                <group>
                    <field name="related_ids" widget="many2many_tags"/>
                </group>

                <!-- Add your fields or attributes here -->
            </group>

        </field>
    </record>

    <record id="mutli_edit_tender_view_tree" model="ir.ui.view">
        <field name="name">_view_tree</field>
        <field name="model">construction.tender</field>
        <field name="arch" type="xml">
            <tree multi_edit="1" editable="bottom">

                <field name="display_type" invisible="1"/>
                <field name="code" attrs="{'required': [('display_type', '=', False)]}" optional="show"/>
                <!--                            <field name="parent_item"/>-->
                <field name="item" attrs="{'required': [('display_type', '=', False)]}" optional="show"/>
                <field name="name" attrs="{'required': [('display_type', '=', False)]}" widget="section_and_note_text"
                       optional="show"/>
                <field name="related_job" optional="show" />
                <field name="uom_id" optional="show"/>
                <field name="qty" optional="show"/>
                <field name="tax_id" optional="show"/>

                <field name="state" readonly="1"/>

                <field name="price_unit" readonly="1"/>
                <field name="total_value" optional="show"/>
                <field name="indirect_cost" readonly="1" optional="show"/>
                <field name="profit" readonly="1" optional="show"/>
                <field name="sale_price" readonly="1" optional="show"/>
                <!--                                    <field name="parent_item" required="1"/>-->

                <field name="notes"/>
            </tree>
        </field>
    </record>

</odoo>